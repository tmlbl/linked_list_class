Linked List Class
=================

An implementation of a linked list class in JavaScript. Uses the following format:

````
{ 
  head: val,
    tail: {
      head: val,
      tail: ..etc
````
To run:

`npm install`

then:

`grunt`
